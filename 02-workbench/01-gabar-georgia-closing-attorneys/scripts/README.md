# Scripts Documentation

Automatically generated per Rule R-131 for scripts in `01-gabar-georgia-closing-attorneys/scripts/`.

---

### `fetch-seed-direct.py`
*   **What it does:** Fetches a comprehensive list of Real Property Law attorneys registered with the Georgia Association of Bar Associations (GABAR) from their public API. It aggregates the data from multiple paginated API calls, transforms nested data into a flat structure, standardizes field names, and saves the collected information into a CSV file.
*   **Why it does it:** To create an initial "seed" dataset of Georgia Closing Attorneys by directly querying the GABAR public API. This ensures a complete and up-to-date collection of relevant professional data for further processing or analysis, while adhering to API usage best practices such as batching and polite delays.
*   **Inputs:** The GABAR public API endpoint `https://api.gabar.org/webservices/membersearch`, queried with parameters for `memberGroup=REALP19` (Real Property Law) and `state=GA` (Georgia).
*   **Outputs:** A CSV file named `01-gabar-georgia-closing-attorneys_seed.csv`, located in the `data` directory two levels above the script's execution path. This file contains detailed information for each attorney, including their address, bar number, company, email, names, sections they belong to, and other profile details.

### `import-seed.py`
*   **What it does:** Imports attorney data from a specified CSV file into a SQLite database. It creates the `attorneys` table if it doesn't exist and inserts new records or updates existing ones based on the `bar_number` field.
*   **Why it does it:** To initialize or update a SQLite database with a foundational dataset of Georgia closing attorneys, likely serving as a seed for a directory or application.
*   **Inputs:**
    *   `../data/01-gabar-georgia-closing-attorneys_seed.csv`: A CSV file containing attorney details (e.g., `id`, `first_name`, `last_name`, `bar_number`, `status`, `admit_date`, `company`, `address1`, `address2`, `city`, `county`, `zip`, `phone`, `email`).
    *   `../data/directory.sqlite`: An existing SQLite database file. If it does not exist, it will be created.
*   **Outputs:**
    *   `../data/directory.sqlite`: The SQLite database file, which is updated with the imported attorney records in the `attorneys` table.

### `p3_background_batch_loop.py`
* **What it does:** Orchestrates a continuous batch processing loop for enriching attorney data. It iteratively calls other scripts to crawl websites, extract and enrich data (including AI processing), validate records, and commit the updated SQLite database to a Git repository until all pending records are processed. It also includes an error correction mechanism for handling unreachable websites.
* **Why it does it:** To automate the entire data pipeline—from initial data acquisition and enrichment to validation and version control—allowing the system to autonomously process a large dataset of attorneys in the background and continuously improve the data quality.
* **Inputs:**
    *   **SQLite Database (`DB_PATH`):** Contains the `attorneys` table with `id`, `website_url`, and `faq_enriched` columns, which are read and updated.
    *   **Cache Directory (`CACHE_DIR`):** Used by `p3_crawl_websites.py` to store crawled text and signal failed crawls via `.failed` files.
    *   **External Python Scripts:** Relies on `p3_crawl_websites.py`, `reset_specialties.py`, `p3_enrich_specialties.py`, `p3_enrich_listings.py`, and `p3_validate_database.py` to perform specific steps of the enrichment process.
    *   **Git Repository:** Assumes the script runs within a Git repository (`WORKSPACE`) to perform `add`, `commit`, and `push` operations.
* **Outputs:**
    *   **Updated SQLite Database (`DB_PATH`):** The `attorneys` table is modified with crawled content, extracted specialties, AI enrichment results (e.g., `faq_enriched` column updated from `NULL` to a processed state or `'FAILED'`), and validation statuses.
    *   **Git Repository Updates:** New commits are created for the updated SQLite database and pushed to the remote repository.
    *   **Console Output:** Logs progress, batch information, and status messages throughout the execution.
    *   **Cache Files (`CACHE_DIR`):** Intermediate crawled text files and `.failed` markers generated by `p3_crawl_websites.py`.

### `p3_enrich_listings.py`
*   **What it does:** This script enriches attorney listings in an SQLite database using the Google Gemini AI model. It fetches attorney records, retrieves previously crawled website content from cache files, generates a prompt and a dynamic JSON schema based on attorney specialties, and then calls the Gemini API to generate structured content (e.g., detailed descriptions, spoken summaries, quick facts, and FAQs). Finally, it updates the attorney records in the database with the AI-generated information.
*   **Why it does it:** To automate the process of generating rich, informative content for attorney profiles within a legal directory, leveraging AI to summarize and extract key details from crawled website data. This enhances the utility and completeness of the directory without manual content creation, making listings more engaging and providing structured data for various directory features.
*   **Inputs:**
    *   **Database:** `data/directory.sqlite` (reads `id`, `first_name`, `last_name`, `firm_name`, `city`, `specialty_wholesale`, `specialty_title_resolution` from the `attorneys` table where `specialties_crawled = 1` and `faq_enriched` is `0` or `NULL`).
    *   **Cache Files:** `data/cache/crawled_text/01-gabar-georgia-closing-attorneys/{id}.txt` (contains raw text content crawled from attorney websites).
    *   **API Key:** `GEMINI_API_KEY` or `GOOGLE_API_KEY` (loaded from environment variables or a `.env` file).
    *   **Command-line Arguments:** `--limit <int>` (optional; limits the number of records to process).
*   **Outputs:**
    *   **Database:** Updates the `attorneys` table in `data/directory.sqlite` by:
        *   Setting `faq_enriched = 1` for successfully processed records.
        *   Setting `faq_enriched = 'FAILED'` for records where a cache file is missing.
        *   Populating new columns with AI-generated content, including `listing_content`, `speakable_what_you_find`, `speakable_listing_details`, `speakable_quick_facts`, `quickfact_best_for`, `quickfact_primary_items`, `quickfact_fee_structure`, `quickfact_access`, `avatar_buyer_faq`, `avatar_investor_faq` (if applicable), and `avatar_defect_faq` (if applicable).
    *   **Console Output:** Logs progress messages for each record, indicating successful enrichment or failures (e.g., "Saved {name}", "Failed on {name}", "Missing cache for {name}").

### `p3_enrich_specialties.py`
*   **What it does:** Scans cached plain text content from attorney websites to identify specific real estate specialties (wholesale, creative finance, land deals, title resolution), languages spoken, accepted payment methods, and trust signals (e.g., wire fraud protocols, accessibility). It then updates an SQLite database with these extracted details for each attorney.
*   **Why it does it:** To enrich the central attorney directory database with detailed, searchable information directly extracted from their web presence. This allows for more granular filtering and discovery of attorneys based on specialized legal services, language capabilities, and operational specifics, building upon previously crawled website data.
*   **Inputs:**
    *   `data/directory.sqlite`: An SQLite database containing `attorneys` records, specifically querying for records where `specialties_crawled` is `0` and a valid `website_url` exists.
    *   `cache/crawled_text/01-gabar-georgia-closing-attorneys/{attorney_id}.txt`: Plain text files containing the crawled content of attorney websites, previously generated by a web scraping script.
    *   `cache/crawled_text/01-gabar-georgia-closing-attorneys/{attorney_id}.failed`: Marker files indicating that the website crawl for a specific attorney previously failed.
    *   Command-line argument `--limit <int>`: An optional integer specifying the maximum number of attorney records to process in a single batch (defaults to 50).
*   **Outputs:**
    *   `data/directory.sqlite`: The `attorneys` table is updated for processed records. Columns updated include: `specialty_wholesale`, `specialty_creative_finance`, `specialty_land_deals`, `specialty_title_resolution` (set to 0 or 1 based on keyword detection); `languages_spoken`, `trust_signals_json`, `accepted_payment_methods` (populated with detected values as JSON strings); and `specialties_crawled` (set to `1` to mark the record as processed).
    *   Standard output (console): Progress messages, details of attorneys being processed, and summaries of detected specialties and languages.

### `p3_enrich_underwriters.py`
* **What it does:** This script connects to a SQLite database, identifies attorney records that currently have no title underwriters specified but do have a website. For these records, it reads their previously crawled website content from a local cache and scans the text for keywords indicating affiliations with major title underwriters (e.g., First American, Fidelity National, Stewart, Old Republic). If underwriters are found, the script updates the attorney's record in the database with this information.
* **Why it does it:** It enriches the attorney directory database by populating the `appointments` field with discovered title underwriter affiliations. This is done "offline" using cached data to avoid re-crawling websites, making the process efficient and allowing for subsequent data analysis or display in a directory. It specifically targets records where no underwriters were previously identified, aiming to fill data gaps.
* **Inputs:**
    * **SQLite Database:** `data/directory.sqlite`, specifically querying the `attorneys` table.
    * **Cached Website Text Files:** `.txt` files located in `cache/crawled_text/01-gabar-georgia-closing-attorneys/` (e.g., `[attorney_id].txt`) containing the full text of previously crawled websites.
    * **Command-line argument `--limit`:** An optional integer specifying the maximum number of records to process in a single batch (default: 50).
* **Outputs:**
    * **SQLite Database:** Updates the `appointments` column in the `attorneys` table for processed records with a JSON array of discovered title underwriters (e.g., `["First American Title"]`).
    * **Standard Output:** Prints progress messages, record IDs being processed, discovered underwriters, and completion status.

### `p3_validate_database.py`
* **What it does:** This script connects to a SQLite database, retrieves attorney records that have been marked as AI-enriched, and applies a set of validation rules. Based on these rules, it determines if a record is suitable for publication or if it needs further work.
* **Why it does it:** To ensure the quality, completeness, and adherence to minimum standards for AI-enriched attorney profiles within the `directory.sqlite` database. It acts as a final quality gate, promoting compliant records to 'publish' status and demoting incomplete or low-quality records to 'draft' status.
* **Inputs:**
    *   **Database:** A SQLite database file named `directory.sqlite` located in the `data` directory, one level up from the script's execution path. This database must contain an `attorneys` table with columns like `id`, `first_name`, `last_name`, `city`, `bar_number`, `listing_content`, `avatar_investor_faq`, `avatar_defect_faq`, `avatar_commercial_faq`, `avatar_buyer_faq`, and `faq_enriched`.
    *   **Command-line argument:** An optional `--limit` (integer) argument to restrict the number of attorney records processed during a validation run.
* **Outputs:**
    *   **Standard Output:** Detailed messages indicating which records passed or failed validation, including specific reasons for failure, and a summary of the total, passed, and failed records.
    *   **Database Modification:** Updates the `post_status` column for processed attorney records in the `attorneys` table of `directory.sqlite`. Records passing all checks are set to 'publish', while failed records are set to 'draft'.

### `reset_specialties.py`
* **What it does:** Reads IDs from text files in a specific cache directory. For each identified ID, it updates the `attorneys` table in a SQLite database, setting the `specialties_crawled` column to `0` for records where the `id` matches and the `faq_enriched` column is `NULL`.
* **Why it does it:** This script likely serves to reset the processing status of attorney specialties. It targets attorneys for whom a cached text file exists (meaning initial data was gathered) but whose `faq_enriched` status is `NULL`, indicating they haven't undergone a subsequent FAQ enrichment process. By setting `specialties_crawled` to `0`, it marks these attorneys for a potential re-crawling or reprocessing of their specialties.
* **Inputs:**
    *   Text files (`.txt`) located in `../cache/crawled_text/01-gabar-georgia-closing-attorneys`. The base filenames (without extension) are treated as attorney IDs.
    *   The `directory.sqlite` database located in `../data`, specifically the `attorneys` table.
* **Outputs:**
    *   Modifies the `specialties_crawled` column in the `attorneys` table within `directory.sqlite`.
    *   Prints a message to standard output indicating the number of records updated or if no cache files were found.